
public class Test {

	/**
	 * @param args
	 */
	public static void main(String[] args) {
		// TODO Auto-generated method stub
		/*
		StringBuilder s = new StringBuilder();
		for( i = 0; i < word.lentgth(); i ++) {
			char c = word.charAt(i);
			if(!((i + 1) < word.length() && word.charAt(i + 1) == '$') || c != '\'') {
				s.append(c);
			}
			
			if(there is an escape) {
				for each char in word
				if regular char 
				add to tempstring
				else if escape
				add $ to tempstring
				else if beggining of variable
				add tempstring to message parts
				word = word substring
			}
			/*myobject
			 * arraylist<string>
			 * 
			 * fillArraylist()
			 * open file
			 * reads data
			 * put into array
			 * 
			 * ArrayList<String> getList()
			 * return list
			 */
			/*main
			 * create instance of object
			 * method fillArrayList
			 * print all items in ArrayList
			 * ArrayList<String> mylist = mo.getList()
			 */
			
				
		
	}

}
